name: hello
description: 'This component is just for testing. No configuration happend, just say hello. No effects for image.'
schemaVersion: 1.0
phases:
  - name: build
    steps:
      - name: SayHello
        action: ExecuteBash
        inputs:
          commands:
            - echo 'Hello, from build instance!'
